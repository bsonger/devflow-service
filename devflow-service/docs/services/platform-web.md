# Platform Web

This file is migrated from `devflow-control` as a cross-repo reference.
It is ownership context only.

## Owns

- browser route composition
- browser-side feature and view state
- orchestrator-facing UI integration

## Does Not Own

- backend domain resources
- backend source-of-truth APIs
- release, verify, or runtime business semantics

## Upstream Dependencies

- `platform-orchestrator`
- browser runtime telemetry
