# Agent Path

This page is index-only.

Canonical agent start:
- `AGENTS.md`

Use `AGENTS.md` for:
- startup order
- authority and routing rules
- current migration focus
- handoff expectations

Use this page only as a navigation alias back to `AGENTS.md`.
